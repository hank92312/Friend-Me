# AdMob 設定範本 — 複製此檔為 ad_config.gd 並填入你的真實廣告單元 ID。
# ad_config.gd 不進版控（見 .gitignore），避免正式 ID 出現在公開 repo。
# 若不建立 ad_config.gd，AdManager 會自動使用 Google 官方測試廣告 ID。
const AD_UNIT_ID := "ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX"
